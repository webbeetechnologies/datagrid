import React from 'react';

export * from 'react-konva';

export const registerCanvasContextBridge = (
    _updatedContexts: React.Context<any> | React.Context<any>[],
) => {};

export const makeRectPath = () => ({});
